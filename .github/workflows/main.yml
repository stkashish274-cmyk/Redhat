name: Force Restart OpenShift VM Every Hour

on:
  schedule:
    - cron: "0 * * * *"   # ⏰ Every hour
  workflow_dispatch:       # ▶️ Allows manual run from GitHub UI

jobs:
  force-restart-vm:
    name: Force Restart OpenShift Virtual Machine
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install OpenShift Client Tools
        run: |
          echo "📦 Installing OpenShift CLI (oc) and virtctl..."
          sudo curl -L https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz -o oc.tar.gz
          sudo tar -xzf oc.tar.gz -C /usr/local/bin oc virtctl
          sudo chmod +x /usr/local/bin/oc /usr/local/bin/virtctl
          oc version || echo "✅ OpenShift CLI installed"

      - name: Login to OpenShift Cluster
        run: |
          echo "🔑 Logging into OpenShift..."
          oc login --token=${{ secrets.OPENSHIFT_TOKEN }} --server=${{ secrets.OPENSHIFT_API_URL }}

      - name: Force Restart VM
        run: |
          VM_NAME="centos-stream9-tan-flea-48"
          NAMESPACE="stkcyber-dev"

          echo "⏹ Stopping VM (if running)..."
          virtctl stop "$VM_NAME" -n "$NAMESPACE" --graceful=false || echo "VM might already be stopped."

          echo "⏳ Waiting 20 seconds before restart..."
          sleep 20

          echo "▶️ Starting VM..."
          virtctl start "$VM_NAME" -n "$NAMESPACE"

          echo "✅ VM $VM_NAME force restarted successfully at $(date)"

      - name: Verify VM Status
        run: |
          VM_NAME="centos-stream9-tan-flea-48"
          NAMESPACE="stkcyber-dev"

          echo "🔍 Checking VM status..."
          virtctl status "$VM_NAME" -n "$NAMESPACE" || echo "⚠️ Status check failed"

          echo "🎯 Restart cycle complete."
