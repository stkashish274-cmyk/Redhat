name: Auto-Restart OpenShift VM (every 5 minutes)

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes
  workflow_dispatch:

jobs:
  keep-vm-running:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare CLI tools (oc + virtctl)
        run: |
          mkdir -p $HOME/bin
          cd $HOME/bin

          echo "📦 Installing OpenShift CLI (compatible)..."
          curl -L -o oc.tar.gz https://mirror.openshift.com/pub/openshift-v4/clients/ocp/4.7.0/openshift-client-linux.tar.gz
          tar -xzf oc.tar.gz
          chmod +x oc kubectl

          echo "📦 Installing virtctl..."
          curl -L -o virtctl https://github.com/kubevirt/kubevirt/releases/download/v0.48.0/virtctl-v0.48.0-linux-amd64
          chmod +x virtctl

          echo "$HOME/bin" >> $GITHUB_PATH

      - name: Login to OpenShift
        env:
          OPENSHIFT_API_URL: ${{ secrets.OPENSHIFT_API_URL }}
          OPENSHIFT_TOKEN: ${{ secrets.OPENSHIFT_TOKEN }}
        run: |
          echo "🔐 Logging in to OpenShift..."
          oc version --client || echo "oc not found in PATH?"
          oc login --token="$OPENSHIFT_TOKEN" --server="$OPENSHIFT_API_URL" --insecure-skip-tls-verify

          echo "📁 Switching to your project..."
          oc project ashisham029-dev

      - name: Check and ensure VM is running
        id: vmcheck
        run: |
          VM_NAME="centos-stream9-orange-swan-23"
          NS="ashisham029-dev"

          echo "🔍 Checking VM status in namespace: $NS ..."
          STATUS=$(oc get vm "$VM_NAME" -n "$NS" -o jsonpath='{.status.printableStatus}' 2>/dev/null || echo "Unknown")
          echo "📡 Current VM status: $STATUS"

          if [ "$STATUS" = "Running" ]; then
            echo "✅ VM is already running."
            echo "vm_status=running" >> $GITHUB_OUTPUT
          else
            echo "⚠️ VM is not running. Attempting restart..."
            virtctl start "$VM_NAME" -n "$NS" || echo "⚠️ Start command failed."
            sleep 30
            STATUS2=$(oc get vm "$VM_NAME" -n "$NS" -o jsonpath='{.status.printableStatus}' 2>/dev/null || echo "Unknown")
            echo "📡 New status: $STATUS2"

            if [ "$STATUS2" = "Running" ]; then
              echo "✅ VM restarted successfully!"
              echo "vm_status=restarted" >> $GITHUB_OUTPUT
            else
              echo "❌ VM did not start properly — check OpenShift console."
              echo "vm_status=failed" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Send Discord Notification
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          STATUS="${{ steps.vmcheck.outputs.vm_status }}"
          if [ -z "$STATUS" ]; then STATUS="unknown"; fi

          case "$STATUS" in
            running)
              MESSAGE="✅ VM \`rhel8-lavender-tiglon-57\` is already running and healthy."
              ;;
            restarted)
              MESSAGE="🔄 VM \`rhel8-lavender-tiglon-57\` was stopped and has been **restarted successfully**."
              ;;
            failed)
              MESSAGE="❌ VM \`rhel8-lavender-tiglon-57\` failed to start. Please check OpenShift console."
              ;;
            *)
              MESSAGE="⚠️ VM status unknown — check manually."
              ;;
          esac

          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"$MESSAGE\"}" \
               "$DISCORD_WEBHOOK"
